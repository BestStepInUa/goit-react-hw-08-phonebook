import styled from 'styled-components';

const AddContactFormStyled = styled.form`
    display: flex;
    flex-direction: column;
    gap: 5px;
    margin-bottom: 25px;
    label {
        display: flex;
        flex-direction: column;
        font-size: 20px;
    }
    input,
    button {
        font-size: 16px;
    }`
    
export default AddContactFormStyled   